version: "3"

services:

      kafka-spark:
        build: .
        networks:
          - mybridge
        depends_on:
          - kafka
        environment:
          KAFKA_BROKE: kafka:9092
          KAFKA_TOPIC: test_topic
        ports:
          - "8080:8080"
          - "4040:4040"
        hostname: kafka-spark
        image: kafka-spark
        container_name: kafka-spark

      zookeeper:
        image: wurstmeister/zookeeper
        networks:
          - mybridge
        ports:
          - "2181:2181"
        container_name: zookeeper
        hostname: zookeeper

      kafka:
        image: wurstmeister/kafka
        networks:
          - mybridge
        depends_on:
          - zookeeper
        ports:
          - "9092:9092"
        environment:
          HOST_IP: kafka
          KAFKA_ADVERTISED_HOST_NAME: kafka
          KAFKA_ADVERTISED_PORT: 9092
          KAFKA_BROKER_ID: 1
          ZK: zookeeper
          KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
          KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
          KAFKA_CREATE_TOPICS: "test_topic,topic_1"
        hostname: kafka
        container_name: kafka





networks:
  mybridge:





